from . import graphsimulator

class ReadSimulator():

    def __init__(self, simulated_graph, n_peaks, with_control=False, self.shift_):
        self.graph = simulated_graph.graph

        self.sample_reads = []
        self.control_reads = []

    def
